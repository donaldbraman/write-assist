Run the repository janitor to clean up stale branches, check for resolved issues, and verify code quality.

Read the janitor agent spec at `.claude/agents/janitor-agent.md` and execute all cleanup tasks.

Configuration:
- Dry run: $ARGUMENTS (pass "dry" for dry run, otherwise execute cleanup)
- Include remote branches: yes
- Check issues: yes

Execute the janitor tasks and provide a summary of actions taken.
